# Agent Workflows

Guidance for repository-specific skill routing.

- **Extension Development**: Use `pi-extension-development` for `packages/pi-*` and `src/extensions/*`.
- **Validation**: Use `pi-validation-flow` for repository edits and registry wiring.
